class Solution {
public:
    bool divisorGame(int n) {
        return !(n & 1);
    }
};

int init = [](){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    return 'c';
}();
